https://crontab.guru/examples.html
https://www.computerhope.com/unix/ucrontab.htm

Used in conjunction with other options, this option allows you to modify or view the crontab file of user. When available, only administrators can use this option.	
	crontab -u [username]	

Install filename as your crontab file. On many systems, this command is executed simply as crontab filename (i.e., without the -a option).
	crontab -a filename	

Edit your crontab file, or create one if it doesn't already exist.
	crontab -e	

Display your crontab file.	fo current user
	crontab -l	
	
Remove your crontab file.
	crontab -r	
	crontab -i
		//Same as -r, but gives the user a yes/no confirmation prompt before removing the crontab.

Display the last time you edited your crontab file. (This option is available on only a few systems.)
	crontab -v	

__________________________________

Crontab files are located in /var/spool/ (or a subdirectory such as /var/spool/cron/crontabs
do not edit this files (root stomper)
	cd var/spool/cron/crontabs
	ls -a
		root
		stomper
__________________________________
	
Each entry in a crontab file consists of six fields, specifying in the following order:
	minute(s) hour(s) day(s) month(s) weekday(s) command(s)

crontab -l
	MAILTO=""
	# Edit this file to introduce tasks to be run by cron.
	#
	# Each task to run has to be defined through a single line
	# indicating with different fields when the task will be run
	# and what command to run for the task
	#
	# To define the time you can provide concrete values for
	# minute (m), hour (h), day of month (dom), month (mon),
	# and day of week (dow) or use '*' in these fields (for 'any').#
	# Notice that tasks will be started based on the cron's system
	# daemon's notion of time and timezones.
	#
	# Output of the crontab jobs (including errors) is sent through
	# email to the user the crontab file belongs to (unless redirected).
	#
	# For example, you can run a backup of all your user accounts
	# at 5 a.m every week with:
	# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
	#
	# For more information see the manual pages of crontab(5) and cron(8)
	#
	# m h  dom mon dow   command
	#>> /var/log/crontab_log.log 2>&1
	30      2       *       *       *       /home/shs/php_bin_console_case_delete.sh
	0       3       */3     *       *       /home/shs/backup_www.sh #>> /var/log/crontab_log.log 2>&1
	0       6       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	5       6       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	10       6       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	15       6       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	20       6       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	25       6       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	30       6       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	0       16       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	5       16       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	10       16       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	15       16       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	20       16       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	25       16       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all
	30       16       *       *       *       /usr/bin/php /var/www/ais.at.kg/bin/console app:ard --em=all

	@daily sh /opt/backup_ais.sh >> /var/log/ais-backup.log 2>&1
