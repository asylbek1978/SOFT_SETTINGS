change an owner of a folder or of a file
	sudo chown username:group directory
	// sudo chown a-abduvapov:a-abduvapov ais.at.kg
	// change recursively
	sudo chown -R a-abduvapov:a-abduvapov ais.at.kg
	sudo chown -R www-data:www-data ais.at.kg
	//stomper user
		src, vendor
	//www-data user
		app, bin, components, tests, var, web w